package ud.main.utils.network;


import ud.main.common.Pair;

public class REST {
    public static String sendRequest(Requests request, String host, int port, Pair... args ) {
        switch (request) {
            case GET: /* todo */
                break;
        }

    }

    public static string
}
